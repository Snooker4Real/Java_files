public class Main {
    public static void main(String[] args) {

        // toString() = special method tht all objects inherit,
        //              that returns a string tht "textually represents" an object.
        //              can be used both implicitly and explicitly

        Car car = new Car();

        //System.out.println(car.toString());
        System.out.println(car);

        

    }
}
